import React from 'react'

import Authentication from '../components/Authentication'

const Header = () => {
  return(
    <div>
      <h1>HOME</h1>
      <Authentication />
    </div>
  );
}

export default Header;